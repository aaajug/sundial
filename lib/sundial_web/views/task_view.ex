defmodule SundialWeb.TaskView do
  use SundialWeb, :view
  # use Phoenix.LiveView, layout: {Sundial.LayoutView, "live.html"}

  # def render(assigns) doout
  #   ~H"""
  #   Current temperature: Hi
  #   """
  # end

  # def handle_params(params, _uri, socket) do
  #   socket =
  #     case params["sort_by"] do
  #       sort_by when sort_by in ~w(deadline) -> assign(socket, sort_by: sort_by)
  #       _ -> socket
  #     end

  #   {:noreply, load_users(socket)}
  # end
end
